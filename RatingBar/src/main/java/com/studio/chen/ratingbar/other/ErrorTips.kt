package com.test.jasonchen.assemblydemo.other

/**
 * Created by marti on 2018/3/16.
 */
val DRAW_ERROR_TIPS = """
                没有设置需要绘制的图形，解决方式：

                                                var builde = jRatingBar.build()

                                                1、builde.build().drawShape = <自定义的图形>
                                                或
                                                2、builde.build().drawShapeType = jRatingBar.DRAW_STAR

                                                builde.build()
            """